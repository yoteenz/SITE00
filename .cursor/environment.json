{
  "name": "SITE 00",
  "user": "ubuntu",
  "install": "npm ci && bash .cursor/scripts/install-cloudflared.sh",
  "start": "bash .cursor/scripts/ensure-site00-preview-url.sh",
  "terminals": [
    {
      "name": "site00-vite",
      "command": "bash .cursor/scripts/run-site00-cloud-preview-server.sh",
      "description": "Production dist on :5174 (vite preview, sync main) — same bundle class as GoDaddy. Set SITE00_CLOUD_PREVIEW_MODE=dev for HMR."
    },
    {
      "name": "site00-preview-tunnel",
      "command": "bash .cursor/scripts/run-site00-preview-tunnel.sh",
      "description": "Cloudflare tunnel to localhost:5174. Auto-restarts on exit; hostname from SITE00_CLOUDFLARE_TUNNEL_HOSTNAME."
    }
  ]
}
