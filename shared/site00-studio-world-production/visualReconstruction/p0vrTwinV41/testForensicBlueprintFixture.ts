/**
 * @deprecated Vitest-only fixture moved to `tests/helpers/twinV41ForensicBlueprintFixture.ts`.
 * Kept as a hard fail if anything in the SPA import graph resolves this module.
 */
export function ensureTwinV41ForensicBlueprintFixturePng(): never {
  throw new Error('testForensicBlueprintFixture is server/test-only — import tests/helpers/twinV41ForensicBlueprintFixture.ts');
}

export function buildFounderApprovedForensicAuthorityForFixture(): never {
  throw new Error('testForensicBlueprintFixture is server/test-only — import tests/helpers/twinV41ForensicBlueprintFixture.ts');
}
