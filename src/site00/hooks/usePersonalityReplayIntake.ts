/**
 * Shadow personality replay intake — re-export from shared context provider.
 */

export {
  PersonalityReplayIntakeProvider,
  usePersonalityReplayIntake,
  type PersonalityReplayIntakeState,
} from './PersonalityReplayIntakeContext.js';
