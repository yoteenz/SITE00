import { AccessCredentialExperience } from '../../components/access/AccessCredentialExperience';

/** Founder Access Credential arrival — /access/:credentialId */
export default function AccessCredentialPage() {
  return <AccessCredentialExperience />;
}
