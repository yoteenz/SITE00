import { Site00EnvironmentViewportBackground } from '../environment/Site00EnvironmentViewportBackground';

/** ENTER 00 desktop — viewport cover layer outside artboard transform. */
export function Site00EnterArtboardViewportBackground() {
  return <Site00EnvironmentViewportBackground environmentId="ENTER_00_WAITING_ROOM" />;
}
