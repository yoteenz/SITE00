/**
 * Sprint B4.4 — Entry 002 storyboard continuity authority pack (temporary until formal pack registered).
 */

import type { ReelStoryboardContinuityAuthority } from '../../../shared/site00-expression-engine/entry002ReelStoryboardTypes.js';

export function buildEntry002StoryboardContinuityAuthority(): ReelStoryboardContinuityAuthority {
  return {
    characterIdentity:
      'NDXBOOK cinematic woman — mid-2010s Instagram baddie silhouette; serves entry not Entry 001 clone',
    hairSilhouette: 'long straight or loose-wave dark hair — consistent across panels when present',
    shortLimeNails: 'short lime-accent nails when hands interact with phone or timeline',
    fashionEvidence: '2016 IG baddie codes: choker, bodycon, bomber, thigh-high boots, overlined nude lip — curated not costume',
    phoneArtifact: 'dark tactile phone — cultural evidence device / archive portal — never comment feed wall',
    nostalgiaEditSuite: 'physical surreal edit suite — timeline strips, cream tape, lime cut markers, light-table depth',
    source: 'FOUNDER_DIRECTED_TEMPORARY',
  };
}
